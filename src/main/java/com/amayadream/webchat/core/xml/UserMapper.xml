<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.amayadream.webchat.core.dao.IUserDao">

    <select id="selectByPrimaryKey" resultType="User" parameterType="string">
        SELECT user_id, nick_name, password, salt, sign, avatar, created_time, updated_time FROM tab_user
        WHERE user_id = #{userId, jdbcType=VARCHAR}
    </select>

    <select id="list" resultType="User">
        SELECT user_id, nick_name, created_time, updated_time FROM tab_user
    </select>

   <insert id="insert" parameterType="User">
       INSERT INTO tab_user (user_id, nick_name, password, salt, sign, avatar, created_time, updated_time)
       VALUES(#{userId, jdbcType=VARCHAR}, #{nickName, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR},
              #{salt, jdbcType=VARCHAR}, #{sign, jdbcType=VARCHAR}, #{avatar, jdbcType=VARCHAR},
              #{createdTime, jdbcType=TIMESTAMP}, #{updatedTime, jdbcType=TIMESTAMP})
   </insert>

    <update id="update" parameterType="User">
        UPDATE tab_user
        <set>
            <if test="nickName != null and nickName != ''">
                nick_name = #{nickName, jdbcType=VARCHAR},
            </if>
            <if test="password != password and password != ''">
                password = #{password, jdbcType=VARCHAR},
            </if>
            <if test="salt != null and salt != ''">
                salt = #{salt, jdbcType=VARCHAR},
            </if>
            <if test="sign != null and sign != ''">
                sign = #{sign, jdbcType=VARCHAR},
            </if>
            <if test="avatar != null">
                avatar = #{avatar, jdbcType=VARCHAR},
            </if>
            <if test="updatedTime != null">
                updated_time = #{updatedTime, jdbcType=TIMESTAMP}
            </if>
        </set>
        WHERE user_id = #{userId, jdbcType=VARCHAR}
    </update>

</mapper>